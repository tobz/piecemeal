[package]
name = "piecemeal-conformance"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Conformance tests for piecemeal code generation"

[dependencies]
piecemeal = { path = "../piecemeal" }
prost = "0.13"

[build-dependencies]
piecemeal-build = { path = "../piecemeal-build" }
prost-build = "0.13"
walkdir = "2"
