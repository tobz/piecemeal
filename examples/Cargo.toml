[package]
name = "examples"
authors = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }
publish = false

[[bin]]
name = "simple"
path = "src/simple/mod.rs"

[dependencies]
base64 = { version = "0.22", default-features = false }
piecemeal = { path = "../piecemeal" }

[build-dependencies]
piecemeal-build = { path = "../piecemeal-build" }
