syntax = "proto3";
package tutorial;

// A simple person message for basic examples.
message Person {
    string name = 1;
    int32 age = 2;
    string email = 3;
}

// A greeting that includes a nested Person message.
message Greeting {
    string message = 1;
    Person sender = 2;
}
